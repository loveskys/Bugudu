import * as login from '@/api/api_login';
import * as authen from '@/api/api_authen';
import * as activity from '@/api/api_activity';
import * as message from '@/api/api_message'

const apis = {
  login,
  authen,
  activity,
  message
}

export default apis